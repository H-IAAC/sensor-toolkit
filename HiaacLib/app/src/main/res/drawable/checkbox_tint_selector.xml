<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor quando o checkbox está marcado -->
    <item android:color="@color/orange_primary" android:state_checked="true" />
    <!-- Cor padrão quando não está marcado (opcional - pode usar cor padrão do sistema) -->
    <item android:color="?android:attr/colorControlNormal" />
</selector>